# 项目维护约定

## 效果视频统一展示

后续添加的效果视频必须沿用已有案例的样式，除非用户明确要求调整整个展示布局。

- `README.md` 和 `README_EN.md`：追加到现有视频预览表格，每行三列，从左往右排列，满三列再换行。末行不足三项时保留空单元格，保持列宽一致。
- 每个案例使用相同的单元格结构：居中、列宽 `33%`、预览封面显示宽度 `240`，上方为可点击的预览封面，下方为商品标题；封面和标题链接到对应的 GitHub Pages 案例详情页。原有预览页视频锚点继续保留。
- 不添加“新增”“New”等标记，不把后添加的视频做成独立大图、单列区块或标题在上的不同布局。
- `index.html`：复用现有 `.gallery` 中的 `.card` 结构、播放器设置、标题和下载入口。沿用电脑三列、手机单列的响应式布局，不为新案例另设样式。
- 同步维护中英文首页、预览页、案例数量、实际视频规格和素材记录。封面取自对应视频，原有案例顺序与样式保持一致。
- 仅添加或调整效果展示时，不修改 Skill 的创作规则、默认参数或已发布版本。

## 案例提示词

- 首页保留统一预览网格，不展开完整提示词。每个案例详情页统一为上方视频、下方对应提示词，提供复制和 TXT 下载入口。
- 作者提供的原始提示词保存在 `assets/prompts/video-XX.txt`，通过 `assets/videos/manifest.json` 的 `original_prompt` 字段与视频绑定。只移除对话外围的括号或代码围栏，不改写正文、品牌、图片引用、时间轴或参数，不用通用模板替换原文。
- 每个详情页由 `python scripts/build_case_pages.py` 根据素材记录与原始 TXT 生成；修改原文或绑定关系后重新生成并核对页面显示、复制文本和下载文件一致。
- 没有提供某个案例的原始提示词时，不编造或推断。案例原文是该次生成的记录，不自动成为通用 Skill 的默认规则。
